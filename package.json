{
  "name": "@snapboard/platform",
  "version": "0.1.0",
  "description": "Snapboard public platform",
  "license": "UNLICENSED",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "clean": "rimraf ./node_modules ./packages/*/node_modules",
    "bootstrap": "lerna bootstrap --use-workspaces"
  },
  "dependencies": {
    "env-cmd": "^10.1.0",
    "lerna": "^4.0.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2"
  },
  "devDependencies": {
  }
}
